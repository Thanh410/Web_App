/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "cdn.dribbble.com",
      "logos-download.com",
      "th.bing.com",
    ],
  },
};

module.exports = nextConfig;
